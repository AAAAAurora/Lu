# 个人主页

这是一个静态个人主页项目，可直接部署到 GitHub Pages。

## 本地预览

直接在浏览器打开 `index.html` 即可。也可以在项目目录运行：

```bash
python3 -m http.server 8000
```

然后访问 `http://localhost:8000`。

## 目录

- `index.html`：页面内容
- `styles/main.css`：页面样式
- `scripts/main.js`：导航高亮
- `assets/`：照片、论文 PDF、流程图、演示视频
